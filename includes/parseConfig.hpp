# ifndef PARSECONFIG_HPP
#define PARSECONFIG_HPP

class PARSECONFIG_HPP
{
    private:
        std::vector<serverConfig>   servers;
        std::vector<std::string>    server_str;
        int                         nbr_server;

    public:     
};

#endif